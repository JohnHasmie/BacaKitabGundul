{
  "name": "classic-book-reader-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Analysis backend for Classic Book Reader — /v1/analyze over SSE, Gemini 2.5 Flash by default. Zero runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test ./test/*.test.js"
  }
}
